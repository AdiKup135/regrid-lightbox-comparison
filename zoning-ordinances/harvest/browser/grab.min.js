(() => { const hex = buf => [...new Uint8Array(buf)].map(b => b.toString(16).padStart(2, "0")).join(""); const b64 = buf => { const bytes = new Uint8Array(buf); let s = ""; for (let i = 0; i < bytes.length; i += 0x8000) s += String.fromCharCode.apply(null, bytes.subarray(i, i + 0x8000)); return btoa(s); }; const part = async (url, buf, http) => ({ url, http, sha256: hex(await crypto.subtle.digest("SHA-256", buf)), b64: b64(buf), }); const sleep = ms => fetch("http://127.0.0.1:8770/wait?ms=" + ms).then(r => r.json()).catch(() => new Promise(r => setTimeout(r, ms))); window.formxGrab = { async fetchParts(urls) { const out = []; for (const url of urls) { const res = await fetch(url, { credentials: "include" }); out.push(await part(url, await res.arrayBuffer(), res.status)); } return out; }, async domPart(selector, url) { const el = document.querySelector(selector); if (!el) throw new Error("no element " + selector); return part(url || location.href, new TextEncoder().encode(el.outerHTML).buffer, 200); }, async send(payload, port = 8770) { const timeout = new Promise((_, rej) => setTimeout(() => rej(new Error("sink timeout (local network access not allowed?)")), 60000)); const res = await Promise.race([fetch(`http://127.0.0.1:${port}/save`, { method: "POST", body: JSON.stringify(payload), headers: { "Content-Type": "text/plain" }, }), timeout]); return res.json(); }, async grab(slug, doc, urls, transport = "chrome-same-origin-fetch") { return this.send({ slug, doc, transport, parts: await this.fetchParts(urls) }); }, async settle(sel = "#codesContent", maxMs = 30000) { let prev = -1; const t0 = Date.now(); while (Date.now() - t0 < maxMs) { const el = document.querySelector(sel); const n = el ? el.innerHTML.length : 0; if (n > 1000 && n === prev) return n; prev = n; await sleep(1500); } return -1; }, async grabDom(slug, doc, sel = "#codesContent") { if (await this.settle(sel) < 0) return { ok: false, error: "content never settled" }; return this.send({ slug, doc, transport: "chrome-rendered-dom", parts: [await this.domPart(sel)] }); }, async gotoNode(nodeId, maxMs = 30000) { const a = [...document.querySelectorAll('a[href*="nodeId="]')] .find(x => x.getAttribute("href").split("nodeId=")[1].split("&")[0] === nodeId); if (a) a.click(); else { history.pushState({}, "", location.pathname + "?nodeId=" + nodeId); window.dispatchEvent(new PopStateEvent("popstate")); } const t0 = Date.now(); let prev = -1; while (Date.now() - t0 < maxMs) { await sleep(1500); const el = document.querySelector("#codesContent"); const hit = el && el.querySelector('div.chunk[id="c_' + nodeId + '"]'); const n = el ? el.innerHTML.length : 0; if (hit && n === prev) return /^Mini TOC/.test(document.title) ? { via: "mini-toc", n: -1 } : { via: a ? "click" : "pushState", n }; prev = n; } return { via: a ? "click" : "pushState", n: -1 }; }, async grabNodes(slug, doc, nodeIds) { const parts = [], log = []; for (const id of nodeIds) { const r = await this.gotoNode(id); log.push(id.split("_").pop() + ":" + r.via + ":" + r.n); if (r.n < 0) return { ok: false, error: "node never rendered " + id, log }; parts.push(await this.domPart("#codesContent", location.origin + location.pathname + "?nodeId=" + id)); } const res = await this.send({ slug, doc, transport: "chrome-rendered-dom", parts, host_marker: this.marker() }); res.log = log; return res; }, marker() { const m = document.body.innerText.match(/VERSION:\s*[A-Z]{3}\s+\d{1,2},\s+\d{4}[^\n]*|(current through|last updated|up to date through)[^\n]{0,120}/i); return m ? m[0].trim() : null; }, async gotoAm(nodeId, maxMs = 30000) { const a = [...document.querySelectorAll(".codenav__toc a, a[href]")].find(x => (x.getAttribute("href") || "").endsWith("/" + nodeId)); if (!a) return { n: -1, via: "no-link" }; a.click(); const t0 = Date.now(); let prev = -1; while (Date.now() - t0 < maxMs) { await sleep(1500); const b = document.querySelector(".codenav__section-body"); const n = b ? b.innerHTML.length : 0; if (location.pathname.endsWith("/" + nodeId) && b && b.querySelector("#rid-" + nodeId) && n === prev) return { n, via: "click" }; prev = n; } return { n: -1, via: "timeout" }; }, async loadAllAm(maxRounds = 40) { const s0 = document.querySelector("#section-0"); const listed = s0 ? s0.innerText.split("\n").filter(l => /^\s*\d+[.\-]\d/.test(l)).length : 0; const count = () => document.querySelectorAll('.codenav__section-body [id^="section-"]').length - 1; let last = -1, still = 0; for (let i = 0; i < maxRounds && count() < listed; i++) { const sc = document.querySelector(".codenav__section-body"); sc.scrollTop = sc.scrollHeight; sc.dispatchEvent(new Event("scroll")); window.scrollTo(0, document.body.scrollHeight); await sleep(1200); if (count() === last) { if (++still > 6) break; } else { still = 0; last = count(); } } return { listed, rendered: count() }; }, async grabAm(slug, doc, nodeIds) { const parts = [], log = []; for (const id of nodeIds) { const r = location.pathname.endsWith("/" + id) && document.querySelector("#rid-" + id) ? { n: 1, via: "here" } : await this.gotoAm(id); if (r.n >= 0) { const c = await this.loadAllAm(); r.via += "/" + c.rendered + "of" + c.listed; if (c.listed && c.rendered < c.listed) r.n = -1; } log.push(id + ":" + r.via + ":" + r.n); if (r.n < 0) return { ok: false, error: "node never rendered " + id, log }; parts.push(await this.domPart(".codenav__section-body", location.href)); } const m = document.body.innerText.match(/(\d{4} S-\d+|Supp\. No\. \d+ - \d{4}) \(current\)/); const res = await this.send({ slug, doc, transport: "chrome-rendered-dom", parts, host_marker: m ? m[0].trim() : null }); res.log = log; return res; }, bg(method, ...args) { window.formxResult = null; this[method](...args).then(r => { window.formxResult = r; }, e => { window.formxResult = { ok: false, error: e.message }; }); return "started"; }, toc(filter = /\| (Chapter|Article|Part|Division|Title|Appendix)/i) { return [...new Set([...document.querySelectorAll('a[href*="nodeId="]')] .map(a => a.getAttribute("href").split("nodeId=")[1].split("&")[0] + " | " + a.textContent.trim().replace(/\s+/g, " ").slice(0, 80)))] .filter(t => filter.test(t)); }, }; return "formxGrab ready"; })(); 
