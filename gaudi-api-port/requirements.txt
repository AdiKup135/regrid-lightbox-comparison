# Python deps of the gaudi-api port (the edge labeler + Phase A setbacks + the
# free parcel provider). Pinned to what gaudi-api already ships.
#
# Render: installed by scripts/render-build-python.sh (pip --target into .pydeps,
# which scripts/render-start.sh puts on PYTHONPATH). See render.yaml.
shapely==2.1.2
requests>=2.31
flask>=3.0
